version: '3.8'
services:
  mysql:
    image: mysql:8.0
    container_name: tienda_toallas_mysql
    restart: always
    environment:
      MYSQL_DATABASE: datatienda
      MYSQL_ROOT_PASSWORD: root
    ports:
      - '3306:3306'
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
